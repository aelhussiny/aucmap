import actions from "./actions";

const updateExtent = actions.updateExtent;
const mapLoaded = actions.mapLoaded;
const setFeatures = actions.setFeatures;


export default {
  updateExtent,
  mapLoaded,
  setFeatures
};
