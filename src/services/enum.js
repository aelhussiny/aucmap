/**
 * This ENUM expresses the REST endpoints for Plat Builder.
 * String substitution using String.replace() should be used to insert variables where applicable.
 */

const API_METHODS = {
  GET_USER: 'user'
};

export default API_METHODS;
